interface:
  display_name: "JJ Bro"
  short_description: "Rewrite the last reply in plain language"
  default_prompt: "Use $jj-bro to restate the last reply in plain human language."
policy:
  allow_implicit_invocation: false
