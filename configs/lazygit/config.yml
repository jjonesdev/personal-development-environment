# https://github.com/jesseduffield/lazygit/blob/master/docs/Config.md
git:
  diffRenderers:
    - name: delta
      colorArg: always
      command: delta --dark --paging=never
keybinding:
  universal:
    confirmInEditor: [<ctrl+g>]
  files:
    commitChangesWithoutHook: w
